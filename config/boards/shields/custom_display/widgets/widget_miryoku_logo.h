#ifndef WIDGET_MIRYOKU_LOGO_H
#define WIDGET_MIRYOKU_LOGO_H

#include "../common.h"

/**
 * Create the Miryoku logo widget
 * 
 * @param parent The parent object to attach the widget to
 * @param x X position
 * @param y Y position
 * @return Pointer to the created widget object
 */
lv_obj_t *widget_miryoku_logo_create(lv_obj_t *parent, int16_t x, int16_t y);

#endif 